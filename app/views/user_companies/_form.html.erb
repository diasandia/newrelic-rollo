<%= form_with(model: user_company, local: true) do |form| %>
  <% if user_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_company.errors.count, "error") %> prohibited this user_company from being saved:</h2>

      <ul>
      <% user_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_company_user_id %>
  </div>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.text_field :company_id, id: :user_company_company_id %>
  </div>

  <div class="field">
    <%= form.label :address_id %>
    <%= form.text_field :address_id, id: :user_company_address_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
