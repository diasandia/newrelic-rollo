<%= form_with(model: companies_address, local: true) do |form| %>
  <% if companies_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(companies_address.errors.count, "error") %> prohibited this companies_address from being saved:</h2>

      <ul>
      <% companies_address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.text_field :company_id, id: :companies_address_company_id %>
  </div>

  <div class="field">
    <%= form.label :address_id %>
    <%= form.text_field :address_id, id: :companies_address_address_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
